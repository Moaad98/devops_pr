apiVersion: apps/v1
kind: Deployment
metadata:
  name: pacman-app
  labels:
    app: pacman
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pacman
  template:
    metadata:
      labels:
        app: pacman
    spec:
      containers:
      - name: pacman
        image: public.ecr.aws/k5u0o7b3/moaad-pacman:latest
        ports:
        - containerPort: 8080
        env:
        - name: MONGO_SERVICE_HOST
          value: "mongo"
        - name: MONGO_DATABASE
          value: "pacman"
        - name: MY_MONGO_PORT
          value: "27017"
        - name: MONGO_USE_SSL
          value: "false"
        - name: MONGO_VALIDATE_SSL
          value: "false"
        - name: MONGO_URL
          value: "mongodb://mongo:27017/pacman"
        # Removed authentication variables since MongoDB isn't configured with auth
        